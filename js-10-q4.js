// What is the result of the code below?
const hello = "what's up";
const obj = {
  func() {
    return `${hello} aps?!`;
  },
};
console.log(obj.func()); // answer is?:  what's up aps?!

// this object has a property called funk, which is a function. First, the func function is called and the function creates the formatted string and returns it.
